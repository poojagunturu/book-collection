{
  "name": "book-collection",
  "version": "1.0.0",
  "description": "* Install Node.js for your OS. Confirm installation was successful by successfully running `node -v` and `npm -v`.\r * Install Electron by running `npm install --save-dev electron`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://git-classes.mst.edu/crsgny/book-collection.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^7.1.1"
  }
}
